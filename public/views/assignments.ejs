<%
if (display) {
    isAdmin = display.isAdmin;
    fields = display.fields;
    headings = display.headings;
    groupNumbers = display.groupNumbers;
    operators = display.operators;
}
if (nav) {
    prevPage = nav.prevPage;
    nextPage = nav.nextPage;
    actionPath = nav.actionPath;
}
if (info) {
    entries = info.entries;
    users = info.users;
    groupData = info.groupData;
    peopleInGroup = info.peopleInGroup;
    message = info.message;
}
%>